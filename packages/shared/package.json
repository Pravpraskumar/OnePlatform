{
  "name": "@platform/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and API contracts for the platform monorepo.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"(no lint configured)\""
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
