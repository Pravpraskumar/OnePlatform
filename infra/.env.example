# SQL Server requires at least eight characters from three of these groups:
# uppercase, lowercase, digits, and symbols. Do not commit the real value.
MSSQL_SA_PASSWORD=ReplaceWithAStrongLocalPassword1!